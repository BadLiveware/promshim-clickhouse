# Measurement and decision — retired shard

Measurement protocol, acceptance rules, rejection rules, and stop/checkpoint
semantics now live in the canonical tracked loop charter:

```text
.pi/plans/layered-optimization-iteration/README.md
```

Attempt-specific evidence belongs in:

```text
harness/artifacts/optimization-iterations/<candidate-id>/notes.md
```

Do not add separate decision thresholds here.
